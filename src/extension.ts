export class GTile {
    gridSizes: GridDims[];
    activeGridSize: GridDims;

    autoClose: boolean;
    animation: boolean;
    showIcon: boolean;
    globalPresetsEnable: bool;

    windowMargin: number;

    insetsPrimary: InsetSpec;
    insetsSecondary: InsetSpec;

    maxTimeout: number;

    constructor() {}
    init() {}
    enable() {}
    disable() {}
}